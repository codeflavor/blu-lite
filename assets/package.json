{
  "name": "Blu-lite",
  "version": "0.0.1",
  "description": "Blu Lite - Frontend assets",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "bower install",
    "build": "browserify src/blulite.js -o build/app.js -t [babelify --presets react]",
    "watch": "watchify src/blulite.js -o build/app.js -t [babelify --presets react] -dv"
  },
  "author": "Victor Palade",
  "repository": "https://github.com/PI-Victor/blu-lite",
  "license": "Apache-2.0",
  "dependencies": {
    "babel-preset-react": "^6.5.0",
    "babelify": "^7.3.0",
    "bower": "^1.7.9",
    "browserify": "^13.0.1",
    "react": "^15.1.0",
    "react-dom": "^15.1.0"
  },
  "devDependencies": {
    "eslint": "^2.11.1",
    "uglify": "^0.1.5",
    "watchify": "^3.7.0"
  }
}
